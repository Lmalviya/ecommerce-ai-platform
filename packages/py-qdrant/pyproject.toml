[project]
name = "py-qdrant"
version = "0.1.0"
description = "Specialized Qdrant client for E-commerce monorepo"
requires-python = ">=3.12"
dependencies = [
    "qdrant-client>=1.7.0",
    "pydantic>=2.5.3",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = true
