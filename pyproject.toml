[project]
name = "ecom-ai-monorepo"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["apps/*", "packages/*"]
exclude = ["packages/py-search-engine"]

[tool.uv]
package = false
