[project]
name = "dev-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "ecom-storage",
]

[tool.uv.sources]
ecom-storage = { path = "../../packages/py-storage" }
