[project]
name = "search-service"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "ecom-shared", # Reference our shared lib
]

[tool.uv.sources]
ecom-shared = { workspace = true }
